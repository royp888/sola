# v1.0.5 合并计划：Bot 设置命令曝光 + 菜单命令优化

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让管理员能在群内直接修改入群验证（`/set_verify` 已存在但未曝光）、补全 BotFather 命令提示列表、补全 `/help` 管理员文本、新增群名自动同步、修复群组面板"私聊工作台"按钮、更新私聊群管中心文案。

**Architecture:**
- `/set_verify type <类型>` 命令已在 `verify.go` 实现但未出现在 BotFather 提示和 `/help` 里 — 只需曝光，不需新增代码。
- 群名同步通过自定义 filter 注册 `new_chat_title` 服务消息 handler，调现有 `ChatBindingService.Bind()`（GORM Assign+FirstOrCreate，nil 指针字段不覆盖）。
- `groupMarkup` 签名改为接受 `botUsername string`，最后一行按钮从 callback 改为 URL，避免群组上下文触发私聊逻辑。

**Tech Stack:** Go 1.25、gotgbot/v2、GORM、现有 bot handler 模式

---

## 文件改动清单

| 文件 | 改动内容 |
|------|---------|
| `internal/bot/bind.go` | 新增 `isNewChatTitle` filter + `handleNewChatTitle` handler；更新 `groupMarkup` 4 处调用为 `groupMarkup(b.User.Username)` |
| `internal/bot/menu.go` | 注册 title handler；修改 `groupMarkup(botUsername string)`；更新 `showGroupMenu`；重写 `groupAdminHelpText` |
| `internal/bot/private.go` | 更新 `showPrivateAdminCenter` 文案，列出可用群内命令含 `/set_verify` |
| `cmd/bot/main.go` | 补全 `groupAdminCommands` 列表，加入 `set_verify`、`set_welcome`、`set_warn_limit`、`violations`、`warns`、`unwarn`、`verify_stats` 等 |

---

### Task 1: 群名自动同步 + 更新 groupMarkup 调用

**Files:**
- Modify: `internal/bot/bind.go`

此 Task 做两件事：① 新增群名变更 handler；② 把 bind.go 里对 `groupMarkup()` 的 4 处调用改为 `groupMarkup(b.User.Username)`（配合 Task 2 的签名变更）。

背景：`ChatBindingService.Bind()` 用 GORM `Assign+FirstOrCreate`，`Username`/`InviteLink`/`Description` 传 `nil` 时不覆盖现有值，因此只传 `ChatID`/`ChatType`/`Title` 可安全更新群名而不影响其他字段。

- [ ] **Step 1: 在 `bind.go` 末尾追加 filter 和 handler**

在 `bind.go` 最后一个函数 `adminPermissionLine` 的结束 `}` 之后追加：

```go
// isNewChatTitle 是 gotgbot message filter，匹配群名变更服务消息。
func isNewChatTitle(msg *gotgbot.Message) bool {
	return msg != nil && msg.NewChatTitle != ""
}

// handleNewChatTitle 在群名变更时自动更新 DB 里存储的标题。
// 只更新 Title 字段，不改变群主归属或其他绑定信息。
// 必须返回 ext.ContinueGroups，否则会阻断后续 handler。
func (a *App) handleNewChatTitle(b *gotgbot.Bot, ctx *ext.Context) error {
	if ctx.Message == nil || ctx.Message.NewChatTitle == "" {
		return ext.ContinueGroups
	}
	if a.services.ChatBindings == nil {
		return ext.ContinueGroups
	}
	scope := requestScope(ctx)
	if scope.Chat.ID == 0 {
		return ext.ContinueGroups
	}
	_, _ = a.services.ChatBindings.Bind(scope.Context, api.ChatBindingRequest{
		ChatID:   scope.Chat.ID,
		ChatType: scope.Chat.Type,
		Title:    ctx.Message.NewChatTitle,
	})
	return ext.ContinueGroups
}
```

`bind.go` 顶部已有 `"github.com/PaulSonOfLars/gotgbot/v2/ext"` 和 `"github.com/dabowin/sola/internal/api"` 的 import，无需新增。

- [ ] **Step 2: 更新 bind.go 里 4 处 `groupMarkup()` 调用**

在 `checkBotAdmin` 函数里，把全部无参数的 `groupMarkup()` 替换为 `groupMarkup(b.User.Username)`。

原文件共 4 处（行号以实际为准）：
1. `"只有群主或管理员可以绑定这个群。..."`那行
2. `fmt.Sprintf("绑定失败：%s", err.Error())`那行
3. `formatBotAdminStatus(scope.Chat, status)+"\n\n绑定成功：..."`那行
4. `formatBotAdminStatus(scope.Chat, status)`那行（最后一行 return）

全部改为 `groupMarkup(b.User.Username)`。

- [ ] **Step 3: 暂不编译（menu.go 还未改签名，Task 2 完成后再编译）**

- [ ] **Step 4: 暂不提交（Task 2 完成后一起提交）**

---

### Task 2: menu.go — 注册 title handler + 修复 groupMarkup + 重写 help 文本

**Files:**
- Modify: `internal/bot/menu.go`

此 Task 做四件事：① `registerCoreHandlers` 注册 title handler；② `groupMarkup` 改为接受 `botUsername string`，最后一行按钮改为 URL；③ `showGroupMenu` 传 `b.User.Username`；④ `groupAdminHelpText` 完整重写。

- [ ] **Step 1: 更新 `registerCoreHandlers`**

把 `registerCoreHandlers` 函数完整替换为：

```go
func (a *App) registerCoreHandlers(d *ext.Dispatcher) {
	d.AddHandler(handlers.NewCommand("start", a.wrap(a.handleStart, a.RateLimit("cmd:start", 1))))
	d.AddHandler(handlers.NewCommand("menu", a.wrap(a.handleStart, a.RateLimit("cmd:menu", 1))))
	d.AddHandler(handlers.NewCommand("help", a.wrap(a.handleHelp, a.RateLimit("cmd:help", 1))))
	d.AddHandler(handlers.NewCommand("info", a.wrap(a.handleInfo, a.RateLimit("cmd:info", 1))))
	d.AddHandler(handlers.NewCommand("html", a.wrap(a.handleHTMLHelp, a.RateLimit("cmd:html", 1))))
	d.AddHandler(handlers.NewCommand("bind", a.wrap(a.handleBind, a.RateLimit("cmd:bind", 1))))
	d.AddHandler(handlers.NewCommand("check_admin", a.wrap(a.handleCheckAdmin, a.RateLimit("cmd:check_admin", 1))))
	d.AddHandler(handlers.NewCommand("cancel", a.wrap(a.handleCancel, a.RateLimit("cmd:cancel", 1))))
	d.AddHandler(handlers.NewMessage(isNewChatTitle, a.handleNewChatTitle))
	d.AddHandler(handlers.NewMessage(message.All, a.handleChineseCommand))
	a.registerSedHandlers(d)
	d.AddHandler(handlers.NewCallback(callbackquery.Prefix(CallbackPrefix+":"), a.wrap(a.router.Handle, a.RateLimit("callback", 1))))
}
```

注意：`isNewChatTitle` 行必须在 `message.All` 之前，否则后者会先截走服务消息。

- [ ] **Step 2: 修改 `groupMarkup` 函数**

把整个 `groupMarkup` 函数替换为：

```go
func groupMarkup(botUsername string) *gotgbot.SendMessageOpts {
	privateURL := "https://t.me/" + botUsername
	return &gotgbot.SendMessageOpts{ReplyMarkup: gotgbot.InlineKeyboardMarkup{InlineKeyboard: [][]gotgbot.InlineKeyboardButton{
		{
			{Text: "💎 我的积分", CallbackData: CallbackData("points", "menu")},
			{Text: "🎁 抽奖大厅", CallbackData: CallbackData("lottery", "active")},
		},
		{
			{Text: "📣 发布中心", CallbackData: CallbackData("publish", "quick")},
			{Text: "⚙️ 群组配置", CallbackData: CallbackData("admin", "config")},
		},
		{
			{Text: "🛡 群管中心", CallbackData: CallbackData("admin", "moderation")},
			{Text: "🔍 关键词", CallbackData: CallbackData("admin", "keywords")},
		},
		{
			{Text: "🏅 等级规则", CallbackData: CallbackData("admin", "levels")},
			{Text: "🔗 邀请链接", CallbackData: CallbackData("admin", "invites")},
		},
		{
			{Text: "📱 私聊工作台", Url: privateURL},
		},
	}}}
}
```

- [ ] **Step 3: 更新 `showGroupMenu` 里的调用**

找到 `showGroupMenu` 函数末尾 `return respondText(...)` 那行，把 `groupMarkup()` 改为 `groupMarkup(b.User.Username)`：

```go
return respondText(b, ctx, strings.Join(lines, "\n"), groupMarkup(b.User.Username))
```

- [ ] **Step 4: 重写 `groupAdminHelpText` 函数**

把整个 `groupAdminHelpText` 函数替换为：

```go
func groupAdminHelpText() string {
	return strings.Join([]string{
		"管理员命令",
		"",
		"-- 成员管理 --",
		"回复消息 /manage — 打开按钮管理面板",
		"/ban 原因 — 封禁（回复消息或 /ban 用户ID 原因）",
		"/unban 用户ID — 解封",
		"/mute 30m — 禁言（30m/2h/1d）",
		"/unmute — 解除禁言",
		"/kick — 踢出",
		"/warn 原因 — 警告  /unwarn — 清除  /warns — 查看记录",
		"/purge — 批量删消息（回复起点消息）",
		"/del — 删除回复的那条消息",
		"/ban_ghosts — 清理已注销账号",
		"/report — 举报（回复目标消息）",
		"/set_title 头衔 — 给管理员设置头衔",
		"/bans — 封禁记录  /violations — 违规记录",
		"",
		"-- 积分 --",
		"回复 /points 10 — 给用户加减积分",
		"/points_config — 查看积分配置",
		"/points_toggle — 开关积分系统",
		"",
		"-- 运营 --",
		"/publish 内容 — 立即发布",
		"/posts — 定时任务列表",
		"/post_create — 创建定时提醒/循环发布",
		"",
		"-- 入群验证 --",
		"/verify_toggle — 开关入群验证",
		"/set_verify type 类型 — 验证类型（button/captcha/multi_choice/poll/math/turnstile）",
		"/set_verify question 问题 — 设置验证问题",
		"/set_verify options [\"A\",\"B\"] — 设置选项（JSON数组）",
		"/verify_stats — 查看验证通过/失败统计",
		"",
		"-- 设置 --",
		"/adminconfig — 查看当前群组配置",
		"/set_welcome 文本 — 设置欢迎语（支持 {name}）",
		"/set_warn_limit 数字 — 设置警告上限（默认 3）",
		"/setrules — 设置群规  /clearrules — 清除  /rules — 查看",
		"/keywords — 关键词规则",
		"/invites — 邀请链接管理",
		"/levels — 等级规则",
	}, "\n")
}
```

- [ ] **Step 5: 确认没有遗漏的旧 `groupMarkup()` 调用（无参数版）**

```powershell
Select-String -Path "C:\Users\Administrator\Desktop\新建文件夹 (5)\TG群管机器人\sola-bot\internal\bot\*.go" -Pattern "groupMarkup\(\)"
```

预期：无输出。

- [ ] **Step 6: 编译验证**

```powershell
cd "C:\Users\Administrator\Desktop\新建文件夹 (5)\TG群管机器人\sola-bot"
go build ./...
```

预期：无输出。

- [ ] **Step 7: 提交 Task 1 + Task 2 的全部改动**

```powershell
git add internal/bot/bind.go internal/bot/menu.go
git commit -m "feat(bot): 群名自动同步；修复群组面板私聊按钮；补全 /help 管理员命令"
```

---

### Task 3: 更新私聊群管中心文案

**Files:**
- Modify: `internal/bot/private.go`

- [ ] **Step 1: 替换 `showPrivateAdminCenter` 函数**

找到整个 `showPrivateAdminCenter` 函数，完整替换为：

```go
func (a *App) showPrivateAdminCenter(b *gotgbot.Bot, ctx *ext.Context, chat api.ChatBinding) error {
	scope := requestScope(ctx)
	lines := []string{
		"🛡 群管中心",
		"",
		fmt.Sprintf("目标：%s", chatTitle(chat)),
		"群组里可直接使用的命令：",
		"  /ban /unban /mute /unmute /kick — 成员管控",
		"  /warn /unwarn /warns — 警告管理",
		"  /purge /del — 消息清理",
		"",
		"群内设置命令：",
		"  /set_welcome 文本 — 设置欢迎语",
		"  /set_warn_limit 数字 — 警告上限",
		"  /set_verify type 类型 — 更改验证类型（button/captcha/math 等）",
		"  /verify_toggle — 开关入群验证",
		"  /adminconfig — 查看完整配置",
	}
	if a.services.TelegramAccess != nil {
		status, err := a.services.TelegramAccess.CheckBotAdmin(scope.Context, b, chat.ChatID)
		if err == nil {
			lines = append(lines, "", fmt.Sprintf("Bot 管理状态：%s", status.Status))
		}
	}
	return respondText(b, ctx, strings.Join(lines, "\n"), privateConsoleMarkup(chat))
}
```

- [ ] **Step 2: 编译验证**

```powershell
go build ./...
```

预期：无输出。

- [ ] **Step 3: 提交**

```powershell
git add internal/bot/private.go
git commit -m "docs(bot): 更新私聊群管中心，列出具体群内命令"
```

---

### Task 4: 补全 BotFather 命令提示列表

**Files:**
- Modify: `cmd/bot/main.go`

当前 `groupAdminCommands` 缺少：`set_welcome`、`set_warn_limit`、`set_verify`、`violations`、`warns`、`unwarn`、`verify_stats`、`purge`、`del`、`ban_ghosts`、`report`、`set_title`。

- [ ] **Step 1: 替换 `groupAdminCommands` 的追加切片**

找到 `groupAdminCommands := append(append([]gotgbot.BotCommand{}, groupMemberCommands...), []gotgbot.BotCommand{` 这段，把整个追加的切片字面量替换为（保留 `}...)` 结尾）：

```go
groupAdminCommands := append(append([]gotgbot.BotCommand{}, groupMemberCommands...), []gotgbot.BotCommand{
	{Command: "ban", Description: "封禁成员（回复消息或接用户ID）"},
	{Command: "unban", Description: "解封 /unban 用户ID"},
	{Command: "mute", Description: "禁言 /mute 30m"},
	{Command: "unmute", Description: "解除禁言"},
	{Command: "kick", Description: "踢出成员"},
	{Command: "warn", Description: "警告成员"},
	{Command: "unwarn", Description: "清除警告（回复目标消息）"},
	{Command: "warns", Description: "查看成员警告记录"},
	{Command: "manage", Description: "打开成员管理面板"},
	{Command: "purge", Description: "批量删消息（回复起点消息）"},
	{Command: "del", Description: "删除回复的那条消息"},
	{Command: "promote", Description: "提升为管理员"},
	{Command: "demote", Description: "撤销管理员权限"},
	{Command: "set_title", Description: "设置管理员头衔"},
	{Command: "report", Description: "举报消息通知管理员"},
	{Command: "ban_ghosts", Description: "清理注销账号"},
	{Command: "bans", Description: "查看封禁记录"},
	{Command: "violations", Description: "查看违规记录"},
	{Command: "setrules", Description: "设置群规"},
	{Command: "clearrules", Description: "清除群规"},
	{Command: "rules", Description: "查看群规"},
	{Command: "publish", Description: "立即发布内容"},
	{Command: "posts", Description: "查看定时任务"},
	{Command: "adminconfig", Description: "查看群组配置"},
	{Command: "set_welcome", Description: "设置欢迎语（支持 {name}）"},
	{Command: "set_warn_limit", Description: "设置警告上限 /set_warn_limit 3"},
	{Command: "set_verify", Description: "更改验证设置 /set_verify type button"},
	{Command: "verify_toggle", Description: "开关入群验证"},
	{Command: "verify_stats", Description: "查看入群验证统计"},
	{Command: "keywords", Description: "查看关键词规则"},
	{Command: "invites", Description: "管理邀请链接"},
	{Command: "levels", Description: "查看等级规则"},
}...)
```

- [ ] **Step 2: 编译验证**

```powershell
go build ./...
```

预期：无输出。

- [ ] **Step 3: 提交**

```powershell
git add cmd/bot/main.go
git commit -m "feat(bot): 补全 BotFather 管理员命令提示列表"
```

---

### Task 5: 推送并发布 v1.0.5

- [ ] **Step 1: 安全扫描（必须执行）**

扫描全仓库，确保无明文 token/密码/密钥：

```powershell
$patterns = @(
    '\d{8,10}:[A-Za-z0-9_-]{35}',
    'BOT_TOKEN\s*=\s*\S+',
    'password\s*=\s*\S{6,}',
    'secret\s*=\s*\S{8,}'
)
Get-ChildItem -Path "C:\Users\Administrator\Desktop\新建文件夹 (5)\TG群管机器人\sola-bot" -Recurse -File |
  Where-Object { $_.Extension -match '\.(go|md|yaml|yml|env|json|txt)$' -and $_.FullName -notmatch '(node_modules|\.git|vendor)' } |
  ForEach-Object {
    $content = Get-Content $_.FullName -Raw -ErrorAction SilentlyContinue
    foreach ($p in $patterns) {
      if ($content -match $p) { Write-Host "FOUND: $($_.FullName)" }
    }
  }
```

预期：无输出（`.env.example` 里的占位符不算）。若有输出，立即清除后再推送。

- [ ] **Step 2: 推送**

```powershell
git push origin main
```

- [ ] **Step 3: 打 tag**

```powershell
git tag v1.0.5
git push origin v1.0.5
```

- [ ] **Step 4: 创建 GitHub Release**

```powershell
gh release create v1.0.5 --title "v1.0.5" --notes "## 新增/修复

- **群名自动同步**：群改名后 DB 里的标题自动更新，后台不再显示旧名称
- **群组面板私聊按钮修复**：「私聊工作台」由 callback 改为 URL 直达私聊，避免私聊逻辑在群聊上下文触发
- **BotFather 命令提示补全**：新增 set_welcome / set_warn_limit / set_verify / violations / warns / unwarn / verify_stats / purge / del / ban_ghosts 等命令提示，用户输入 / 时可见
- **\`/help\` 管理员版重写**：新增「入群验证」分节（/set_verify type 用法）；补全 purge / del / ban_ghosts / report / set_title / verify_stats 等遗漏命令
- **私聊群管中心文案更新**：直接列出可用的群内命令，含 /set_verify type"
```

- [ ] **Step 5: 更新 README changelog**

在 `README.md` 更新日志最顶部插入：

```
- **2026-06-18** v1.0.5 — 群名自动同步；修复群组面板私聊按钮(URL)；补全 BotFather 命令提示和 /help 管理员命令列表；更新私聊群管中心文案
```

在 `README.en.md` 更新日志最顶部插入：

```
- **2026-06-18** v1.0.5 — Auto-sync chat title on rename; fix group panel private-chat button (URL); complete BotFather command hints and /help admin list; update private admin center copy
```

```powershell
git add README.md README.en.md
git commit -m "docs: 更新 v1.0.5 更新日志"
git push origin main
```
